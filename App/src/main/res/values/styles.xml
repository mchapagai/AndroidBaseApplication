<resources>
    <style name="HorizontalDivider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/margin_1dp</item>
        <item name="android:background">?attr/colorPrimaryRed</item>
    </style>

    <style name="ActionBar.Text" parent="@android:style/TextAppearance.Material.Widget.ActionBar.Title">
        <item name="android:textColor">?attr/colorPrimaryRed</item>
        <item name="android:textSize">@dimen/default_medium_text</item>
    </style>

</resources>
